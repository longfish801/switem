
// 例外メッセージ
exc {
	invalidHierarchy = 'Invalid hierarchy of tags, check syntax. tag=%s upper=%s'
	noTagDslDoc = 'No clmap document of tagdsl is defined. tag=%s upper=%s'
	invalidTag = 'Invalid tag, check syntax. tag=%s upper=%s'
	faileScript = 'Failed to run switem script. key=%s'
	noEndLine = 'No end-line exists for the begin-line. begin-line=%s'
	invalidKeyType = 'Invalid type of key value, check syntax. key=%s value=%s'
	noKeyRequired = 'No key is specified. It is required. key=%s'
	invalidRangeSize = 'Too big size of range. lineIdx=%d, size=%d, opeKey=%s'
	noOnelineKey = 'Neither bullet key nor pattern key  for oneline handle'
	invalidOutRange = 'Invalid out key. out=%s'
}

// ログメッセージ
log {
	failedCompile = 'Failed to compile closure. clpath={}, code=\n{}\n-----'
}
